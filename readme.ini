def print_grid(numbers):
  """Виводить квадрат чисел."""
  for row in numbers:
    print(" ".join(str(n) for n in row))

def main():
  """Основна функція."""
  numbers = generate_random_numbers()
  print_grid(numbers)

if __name__ == "__main__":
  main()
